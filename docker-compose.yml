services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: portuguese_quiz
      POSTGRES_USER: quiz_user
      POSTGRES_PASSWORD: root
    volumes:
      - postgres_data:/var/lib/postgresql/data

  web:
    build: .
    environment:
      DATABASE_URL: postgresql://quiz_user:root@db:5432/portuguese_quiz
      SECRET_KEY: quiz-secret-key
    depends_on:
      - db

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - web

volumes:
  postgres_data:
